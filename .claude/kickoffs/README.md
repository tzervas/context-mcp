# context-mcp local kickoffs

Main brief: root `.claude/kickoffs/ctx.md` (owns this tree disjointly).

Apply: real RAG gates, honesty (no pseudo claims), Wave 0-3 from ROADMAP.

Tero + dev-workflow. Per-repo dev PRs.
